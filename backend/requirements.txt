# Versions already present in the Conda environment used to train the models.
# Keeping these exact prevents pip from replacing the known-good ML runtime.
torch==2.5.1
torchvision==0.20.1
ultralytics==8.4.104
Pillow==11.1.0
numpy>=1.26.0,<3.0.0
scikit-image>=0.24.0,<1.0.0

# Web API packages that must be added to the existing `rice` environment.
fastapi>=0.115.0,<1.0.0
uvicorn>=0.30.0,<1.0.0
python-multipart>=0.0.9,<1.0.0
httpx>=0.27.0,<1.0.0
python-dotenv>=1.0.0,<2.0.0

# Accounts, persistence, and stored prediction images.
pymongo>=4.13.0,<5.0.0
cloudinary>=1.41.0,<2.0.0
PyJWT>=2.9.0,<3.0.0
pwdlib[argon2]>=0.2.1,<1.0.0
email-validator>=2.2.0,<3.0.0
